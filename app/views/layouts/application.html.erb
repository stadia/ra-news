<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-56PSNXG7QG"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-56PSNXG7QG');
    </script>
    <title><%= content_for(:title) || "RA || 루비 AI 뉴스" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#059669">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

    <!-- SEO Meta Tags -->
    <meta name="description" content="최신 Ruby, Rails, AI 관련 뉴스와 트렌드를 한곳에서 만나보세요">
    <meta name="keywords" content="Ruby, Rails, AI, 프로그래밍, 개발, 뉴스">
    <meta name="author" content="RA || 루비 AI 뉴스">

    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= content_for(:title) || "RA || 루비 AI 뉴스" %>">
    <meta property="og:description" content="최신 Ruby, Rails, AI 관련 뉴스와 트렌드를 한곳에서 만나보세요">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="RA || 루비 AI 뉴스">

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for(:title) || "RA || 루비 AI 뉴스" %>">
    <meta name="twitter:description" content="최신 Ruby, Rails, AI 관련 뉴스와 트렌드를 한곳에서 만나보세요">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Enable CSS View Transitions API for smooth page transitions %>
    <meta name="view-transition" content="same-origin">
    
    <%# bfcache optimization - prevent cache blocking %>
    <meta name="format-detection" content="telephone=no">
    <meta name="apple-mobile-web-app-title" content="RA News">
    
    <%# Speculation Rules for instant navigation %>
    <script type="speculationrules">
    {
      "prerender": [{
        "where": { "href_matches": "/*" },
        "eagerness": "moderate"
      }],
      "prefetch": [{
        "where": { "href_matches": "/*" },
        "eagerness": "conservative"
      }]
    }
    </script>
    
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <%# Modern CSS Navigation with minimal JavaScript %>
    <script src="<%= asset_path('modern_navigation.js') %>" defer></script>
  </head>

  <body class="bg-gray-900 text-gray-200 min-h-screen flex flex-col bfcache-optimized">

    <!-- CSS-only Scroll Progress Indicator -->
    <div class="scroll-progress"></div>

    <!-- CSS-only Loading Indicator with View Transitions -->
    <div class="loading-indicator fixed inset-0 bg-gray-900 bg-opacity-75 z-50 hidden items-center justify-center" 
         style="view-transition-name: loading;">
      <div class="flex flex-col items-center space-y-4">
        <div class="animate-spin rounded-full h-12 w-12 border-4 border-green-500 border-t-transparent"></div>
        <div class="text-white font-medium">로딩 중...</div>
      </div>
    </div>

    <!-- Modern CSS-only Navigation -->
    <nav class="bg-green-700 border-green-800 navigation" style="view-transition-name: navigation;">
      <div class="max-w-screen-xl nav-container mx-auto p-4">
        <%= link_to root_path, class: "flex items-center space-x-3 rtl:space-x-reverse" do %>
          <span class="self-center text-2xl font-semibold whitespace-nowrap text-white">RA || 루비 AI 뉴스</span>
        <% end %>
        
        <!-- CSS-only Mobile Menu Toggle -->
        <input type="checkbox" id="mobile-menu-toggle" class="mobile-menu-toggle" aria-hidden="true">
        <label for="mobile-menu-toggle" class="mobile-menu-button inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-100 rounded-lg md:hidden hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-500 cursor-pointer" aria-controls="navbar-search">
          <span class="sr-only">메인 메뉴 열기</span>
          <svg class="menu-icon-open w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 1h15M1 7h15M1 13h15"/>
          </svg>
          <svg class="menu-icon-close w-5 h-5 hidden" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
          </svg>
        </label>
        
        <div class="mobile-menu items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-search">
          <ul class="nav-items flex flex-col p-4 md:p-0 mt-4 font-medium border border-green-600 rounded-lg bg-green-600 md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 md:bg-green-700">
            <li class="nav-item">
              <%= link_to "홈", root_path, class: "nav-link block py-2 px-3 text-gray-100 hover:text-white rounded-sm md:hover:text-white md:p-0", "aria-current" => "page" %>
            </li>
            <li class="nav-item">
              <%= link_to "지난 글", articles_path, class: "nav-link block py-2 px-3 text-gray-100 hover:text-white rounded-sm md:hover:text-white md:p-0", "aria-current" => "page" %>
            </li>
            <li class="nav-item flex items-center">
              <%= form_with url: articles_path, method: :get, local: true, class: "flex items-center space-x-2" do |form| %>
                <%= form.text_field :search,
                    placeholder: "검색...",
                    value: params[:search],
                    class: "px-3 py-1 text-sm text-gray-900 bg-white border border-gray-300 rounded-lg focus:ring-green-500 focus:border-green-500 w-32 md:w-40" %>
                <%= form.submit "검색",
                    class: "px-3 py-1 text-sm font-medium text-white bg-green-600 rounded-lg border border-green-600 hover:bg-green-500 focus:ring-4 focus:outline-none focus:ring-green-300" %>
              <% end %>
            </li>
            <% if authenticated? %>
              <li class="nav-item">
                <%= link_to "글 등록", new_article_path, class: "nav-link block py-2 px-3 text-gray-100 hover:text-white rounded-sm md:hover:text-white md:p-0", "aria_current" => "page" %>
              </li>
              <li class="nav-item">
                <span class="block py-2 px-3 text-gray-100 rounded-sm md:p-0" aria-current="page"><%= Current.user.name %></span>
              </li>
            <% end %>
            <li class="nav-item">
              <% if authenticated? %>
                <%= link_to "로그아웃", logout_path, class: "nav-link block py-2 px-3 text-gray-100 hover:text-white rounded-sm md:hover:text-white md:p-0", "aria_current" => "page" %>
              <% else %>
                <%= link_to "로그인", new_session_path, id: "loginLink", class: "nav-link block py-2 px-3 text-gray-100 hover:text-white rounded-sm md:hover:text-white md:p-0", "aria_current" => "page" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4 py-8 flex-grow">
        <%= yield %>
    </main>

    <footer class="bg-green-700 text-gray-200 rounded-lg shadow-sm m-4">
        <div class="w-full mx-auto max-w-screen-xl p-4 md:flex md:items-center md:justify-between">
          <span class="text-sm text-gray-300 sm:text-center">© 2025 <a href="https://flowbite.com/" class="hover:underline hover:text-white">RA || 루비 AI 뉴스</a>. All Rights Reserved.</span>
          <ul class="flex flex-wrap items-center mt-3 text-sm font-medium text-gray-300 sm:mt-0">
          </ul>
        </div>
    </footer>
  </body>
  <style>
      @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap');
  </style>
</html>
