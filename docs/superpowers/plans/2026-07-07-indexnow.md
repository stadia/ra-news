# IndexNow 기사 이벤트 ping 확장 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기사가 confirmed 상태로 진입하거나 의미 있는 콘텐츠가 변경될 때, ko(`.dev`)와 ja(`.jp`) 양쪽 호스트의 공개 URL을 IndexNow에 ping한다.

**Architecture:** `Article` `after_commit` 콜백이 per-(article,host) 캐시 잠금으로 `IndexNowJob`을 30s 지연 예약(디바운스). 잡은 기사 공개 URL을 계산해 `IndexNowService`로 전송. `IndexNowService`는 기존 `ArticleBatchJob#ping_index_now` 로직을 이전한 단일 진실 원천이며 배치 잡도 같이 사용. 키/호스트는 `Hosts` 모듈 상수에서 파생.

**Tech Stack:** Rails 8.1.3, Ruby 4.0.5, ActiveJob + Solid Queue, solid_cache(`Rails.cache`), Faraday 2.x, Minitest(`bin/rails test`), rbs_inline.

## Global Constraints

- 테스트 러너: `bin/rails test` (Minitest). CI가 `.github/workflows/ci.yml:98`에서 사용.
- 모든 서비스/잡/모델 파일: `# frozen_string_literal: true` + `# rbs_inline: enabled` 헤더 + 메서드 시그니처 주석(`#: (...) -> ...`).
- IndexNow API 엔드포인트: `https://api.indexnow.org/IndexNow` (그대로 유지).
- IndexNow 키값: `187d5ed120cc45f8869b89302011d43a` (기존, `public/187d5ed120cc45f8869b89302011d43a.txt`와 일치).
- credentials 사용 금지 — 키는 `Hosts::INDEX_NOW_KEY` 상수(사용자 결정).
- 비재시도 정책 — 서비스/잡 모두 실패 시 로그만, 재시도/백오프 없음.
- 모델 인스턴스 `confirmed?` 메서드는 **없음**(scope만 존재). confirmed 판정은 `slug.present? && title_ko.present?` 인라인.
- `kept?`는 `Discard::Model`에서 제공(`include Discard::Model` at `app/models/article.rb:22`).
- 매 에디트 후 `rails 'ai:tool[validate]' files=...` 실행.

---

## File Structure

- **Create** `app/services/index_now_service.rb` — 호스트별 URL 리스트 → IndexNow POST. void + 로그. plain class(OperationService 미상속).
- **Create** `app/jobs/index_now_job.rb` — `(article_id, host)` 인자. 기사 조회·공개 URL 계산·서비스 호출·잠금 해제. `ApplicationJob` 상속.
- **Create** `test/functions/hosts_test.rb` — `Hosts::INDEX_NOW_KEY`, `INDEX_NOW_HOSTS` 단정.
- **Create** `test/services/index_now_service_test.rb` — 서비스 동작 단정(Faraday.stub).
- **Create** `test/jobs/index_now_job_test.rb` — 잡 동작 단정.
- **Modify** `app/functions/hosts.rb` — `INDEX_NOW_KEY`, `INDEX_NOW_HOSTS` 상수 추가.
- **Modify** `app/models/article.rb` — `after_commit :enqueue_index_now, on: [:create, :update]` + private `enqueue_index_now` + `INDEX_NOW_WATCHED_ATTRIBUTES` 상수.
- **Modify** `test/models/article_test.rb` — IndexNow enqueue/디바운스 단정 추가.
- **Modify** `app/jobs/article_batch_job.rb` — `ping_index_now` 삭제, `IndexNowService` 직접 호출로 교체.
- **Modify** `test/jobs/article_batch_job_test.rb` — `ping_index_now` stub → `IndexNowService` stub.

---

### Task 1: Hosts 모듈에 IndexNow 상수 추가

**Files:**
- Modify: `app/functions/hosts.rb` (상수 영역, `DEFAULT` 근처)
- Test: `test/functions/hosts_test.rb`

**Interfaces:**
- Produces: `Hosts::INDEX_NOW_KEY` (String `"187d5ed120cc45f8869b89302011d43a"`), `Hosts::INDEX_NOW_HOSTS` (Array `["ruby-news.dev", "ruby-news.jp"]`).

- [ ] **Step 1: Write the failing test**

Create `test/functions/hosts_test.rb`:

```ruby
# frozen_string_literal: true

require "test_helper"

class HostsTest < ActiveSupport::TestCase
  test "INDEX_NOW_KEY는 public 키 파일명과 일치하는 문자열이다" do
    assert_equal "187d5ed120cc45f8869b89302011d43a", Hosts::INDEX_NOW_KEY
    assert File.exist?(Rails.public_path.join("#{Hosts::INDEX_NOW_KEY}.txt"))
  end

  test "INDEX_NOW_HOSTS는 FOR_LOCALE에서 파생된 호스트 목록이다" do
    assert_equal %w[ruby-news.dev ruby-news.jp], Hosts::INDEX_NOW_HOSTS
  end
end
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bin/rails test test/functions/hosts_test.rb`
Expected: FAIL — `NameError: uninitialized constant Hosts::INDEX_NOW_KEY`

- [ ] **Step 3: Write minimal implementation**

In `app/functions/hosts.rb`, add after the `DEFAULT = "https://ruby-news.dev"` line (line 15):

```ruby
  # IndexNow 인증 키. public/<key>.txt 파일이 같은 키값으로 서빙된다.
  # 두 도메인(.dev/.jp)이 같은 Rails 앱·같은 public/을 공유하므로 단일 파일로 충분.
  INDEX_NOW_KEY = "187d5ed120cc45f8869b89302011d43a".freeze

  # IndexNow ping 대상 호스트(스킴 제외). FOR_LOCALE에서 자동 파생.
  INDEX_NOW_HOSTS = FOR_LOCALE.values.map { |url| URI(url).host }.freeze
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bin/rails test test/functions/hosts_test.rb`
Expected: PASS (2 runs, 0 failures)

- [ ] **Step 5: Validate**

Run: `rails 'ai:tool[validate]' files=app/functions/hosts.rb level=rails`
Expected: no errors

- [ ] **Step 6: Commit**

```bash
git add app/functions/hosts.rb test/functions/hosts_test.rb
git commit -m "Add IndexNow key and host constants to Hosts module

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: IndexNowService 생성

**Files:**
- Create: `app/services/index_now_service.rb`
- Test: `test/services/index_now_service_test.rb`

**Interfaces:**
- Consumes: `Hosts::INDEX_NOW_KEY` (from Task 1)
- Produces: `IndexNowService#call(host:, urls:)` → void. 시그니처 `#: (host: String, urls: Array[String]) -> void`. Faraday POST to `https://api.indexnow.org/IndexNow`.

- [ ] **Step 1: Write the failing test**

Create `test/services/index_now_service_test.rb`:

```ruby
# frozen_string_literal: true

require "test_helper"

class IndexNowServiceTest < ActiveSupport::TestCase
  setup do
    @service = IndexNowService.new
  end

  test "urls가 비어 있으면 API를 호출하지 않는다" do
    called = false
    Faraday.stub(:post, ->(*) { called = true; Struct.new(:status, :body).new(200, "") }) do
      @service.call(host: "ruby-news.dev", urls: [])
    end
    assert_not called, "빈 urls로 Faraday.post가 호출되면 안 됨"
  end

  test "Hosts::INDEX_NOW_KEY가 비어 있으면 API를 호출하지 않는다" do
    called = false
    Faraday.stub(:post, ->(*) { called = true; Struct.new(:status, :body).new(200, "") }) do
      Hosts.stub(:const_get, ->(name) { name == :INDEX_NOW_KEY ? "" : Hosts.const_get(name) }) do
        @service.call(host: "ruby-news.dev", urls: [ "https://ruby-news.dev/articles/x" ])
      end
    end
    assert_not called
  end

  test "200/202 응답은 성공으로 로깅한다" do
    Faraday.stub(:post, ->(*) { Struct.new(:status, :body).new(202, "") }) do
      assert_nothing_raised { @service.call(host: "ruby-news.jp", urls: [ "https://ruby-news.jp/articles/x" ]) }
    end
  end

  test "422 응답은 에러 로깅하고 raise하지 않는다" do
    Faraday.stub(:post, ->(*) { Struct.new(:status, :body).new(422, "bad key") }) do
      assert_nothing_raised { @service.call(host: "ruby-news.dev", urls: [ "https://ruby-news.dev/articles/x" ]) }
    end
  end

  test "Faraday 예외는 에러 로깅하고 raise하지 않는다" do
    Faraday.stub(:post, ->(*) { raise Faraday::ConnectionFailed, "timeout" }) do
      assert_nothing_raised { @service.call(host: "ruby-news.dev", urls: [ "https://ruby-news.dev/articles/x" ]) }
    end
  end

  test "host에 따라 keyLocation가 올바르게 파생된다" do
    captured = nil
    Faraday.stub(:post, lambda { |url, body, _headers = nil|
      payload = JSON.parse(body)
      captured = payload
      Struct.new(:status, :body).new(200, "")
    }) do
      @service.call(host: "ruby-news.jp", urls: [ "https://ruby-news.jp/articles/x" ])
    end
    assert_equal "ruby-news.jp", captured["host"]
    assert_equal Hosts::INDEX_NOW_KEY, captured["key"]
    assert_equal "https://ruby-news.jp/#{Hosts::INDEX_NOW_KEY}.txt", captured["keyLocation"]
    assert_equal [ "https://ruby-news.jp/articles/x" ], captured["urlList"]
  end
end
```

> 참고: `Faraday.stub(:post)` 블록 시그니처는 `(url, &block)` 또는 `(url, body, headers)` — `discord_client_test.rb`에서 `(url, &block)` 패턴을 사용하지만, 본 서비스는 `Faraday.post(url, body, headers)` 3-인자 호출이므로 위 lambda가 일치. 구현체의 Faraday 호출 시그니처와 정확히 맞춤(Step 3 참고).

- [ ] **Step 2: Run test to verify it fails**

Run: `bin/rails test test/services/index_now_service_test.rb`
Expected: FAIL — `NameError: uninitialized constant IndexNowService`

- [ ] **Step 3: Write minimal implementation**

Create `app/services/index_now_service.rb`:

```ruby
# frozen_string_literal: true
# rbs_inline: enabled

# IndexNow API로 URL 리스트를 전송한다.
# 호스트별 keyLocation(https://<host>/<key>.txt)를 포함해 POST.
# 성공/실패 모두 로그만 남기고 raise하지 않는다(비재시도 정책).
class IndexNowService
  ENDPOINT = "https://api.indexnow.org/IndexNow"

  #: (host: String, urls: Array[String]) -> void
  def call(host:, urls:)
    return if urls.blank?

    key = Hosts::INDEX_NOW_KEY
    return if key.blank?

    payload = {
      host: host,
      key: key,
      keyLocation: "https://#{host}/#{key}.txt",
      urlList: urls
    }

    response = Faraday.post(
      ENDPOINT,
      payload.to_json,
      { "Content-Type" => "application/json; charset=utf-8" }
    )

    if response.status.to_i.between?(200, 299)
      Rails.logger.info("IndexNow ping success: host=#{host} urls=#{urls.size}")
    else
      Rails.logger.error("IndexNow ping failed: host=#{host} status=#{response.status} body=#{response.body}")
    end
  rescue StandardError => e
    Rails.logger.error("IndexNow ping error: host=#{host} #{e.class} - #{e.message}")
  end
end
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bin/rails test test/services/index_now_service_test.rb`
Expected: PASS (6 runs, 0 failures)

> 만약 `Faraday.stub` lambda 인자 수 불일치 에러 발생 시, `Faraday.post(url, body, headers)` 3-인자에 맞춰 lambda를 `lambda { |url, body, headers| ... }`로 조정. 단, 기존 `discord_client_test.rb:83`의 `(url, &block)` 패턴과 다름에 유의 — 본 서비스는 `Faraday.post(3인자)` 직접 호출이므로 3-인자 lambda가 맞음.

- [ ] **Step 5: Validate**

Run: `rails 'ai:tool[validate]' files=app/services/index_now_service.rb level=rails`
Expected: no errors

- [ ] **Step 6: Commit**

```bash
git add app/services/index_now_service.rb test/services/index_now_service_test.rb
git commit -m "Add IndexNowService for host-based URL pinging

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: IndexNowJob 생성

**Files:**
- Create: `app/jobs/index_now_job.rb`
- Test: `test/jobs/index_now_job_test.rb`

**Interfaces:**
- Consumes: `IndexNowService#call(host:, urls:)` (Task 2), `Hosts::INDEX_NOW_KEY` (Task 1)
- Produces: `IndexNowJob.perform_later(article_id, host)`. 잡 내부 캐시 잠금 키 `"index_now:enqueue:#{host}:#{article_id}"`를 `ensure`에서 삭제.

- [ ] **Step 1: Write the failing test**

Create `test/jobs/index_now_job_test.rb`:

```ruby
# frozen_string_literal: true

require "test_helper"

class IndexNowJobTest < ActiveSupport::TestCase
  setup do
    @article = articles(:ruby_article) # slug + title_ko present → confirmed
  end

  test "confirmed 기사는 호스트별 공개 URL로 서비스를 호출한다" do
    called = []
    fake_service = Object.new
    fake_service.define_singleton_method(:call) do |host:, urls:|
      called << { host:, urls: }
    end
    IndexNowService.stub(:new, -> { fake_service }) do
      IndexNowJob.new.perform(@article.id, "ruby-news.jp")
    end

    assert_equal 1, called.size
    assert_equal "ruby-news.jp", called.first[:host]
    assert_includes called.first[:urls].first, "ruby-news.jp/articles/"
    assert_includes called.first[:urls].first, @article.slug
  end

  test "unconfirmed 기사(slug 누락)는 서비스를 호출하지 않는다" do
    @article.update_columns(slug: nil)

    called = false
    fake_service = Object.new
    fake_service.define_singleton_method(:call) do |host:, urls:|
      called = true
    end
    IndexNowService.stub(:new, -> { fake_service }) do
      IndexNowJob.new.perform(@article.id, "ruby-news.dev")
    end
    assert_not called
  end

  test "삭제된 기사(id 조회 실패)는 서비스를 호출하지 않고 예외도 발생하지 않는다" do
    called = false
    fake_service = Object.new
    fake_service.define_singleton_method(:call) do |host:, urls:|
      called = true
    end
    IndexNowService.stub(:new, -> { fake_service }) do
      assert_nothing_raised { IndexNowJob.new.perform(-999_999, "ruby-news.dev") }
    end
    assert_not called
  end

  test "perform 후 캐시 잠금 키를 삭제한다" do
    lock_key = "index_now:enqueue:ruby-news.dev:#{@article.id}"
    Rails.cache.write(lock_key, true, expires_in: 60.seconds)

    fake_service = Object.new
    fake_service.define_singleton_method(:call) { |host:, urls:| }
    IndexNowService.stub(:new, -> { fake_service }) do
      IndexNowJob.new.perform(@article.id, "ruby-news.dev")
    end

    assert_not Rails.cache.exist?(lock_key)
  end
end
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bin/rails test test/jobs/index_now_job_test.rb`
Expected: FAIL — `NameError: uninitialized constant IndexNowJob`

- [ ] **Step 3: Write minimal implementation**

Create `app/jobs/index_now_job.rb`:

```ruby
# frozen_string_literal: true
# rbs_inline: enabled

# 기사 공개 URL을 IndexNow에 ping한다.
# Article after_commit에서 per-(article,host) 캐시 잠금과 함께 wait: 30s로 예약됨.
# 지연 실행 중 기사가 삭제되거나 unconfirmed가 되면 전송을 스킵한다.
class IndexNowJob < ApplicationJob
  queue_as :default

  #: (Integer article_id, String host) -> void
  def perform(article_id, host)
    article = Article.kept.find_by(id: article_id)
    return unless article
    return if article.slug.blank? || article.title_ko.blank?

    url = Rails.application.routes.url_helpers.article_url(
      article,
      host: host,
      protocol: "https"
    )
    IndexNowService.new.call(host: host, urls: [ url ])
  ensure
    Rails.cache.delete("index_now:enqueue:#{host}:#{article_id}")
  end
end
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bin/rails test test/jobs/index_now_job_test.rb`
Expected: PASS (4 runs, 0 failures)

- [ ] **Step 5: Validate**

Run: `rails 'ai:tool[validate]' files=app/jobs/index_now_job.rb level=rails`
Expected: no errors

- [ ] **Step 6: Commit**

```bash
git add app/jobs/index_now_job.rb test/jobs/index_now_job_test.rb
git commit -m "Add IndexNowJob to ping a single article URL per host

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: Article after_commit 콜백 + 디바운스

**Files:**
- Modify: `app/models/article.rb` (콜백 영역 line 111-153 근처, private 영역)
- Test: `test/models/article_test.rb` (IndexNow 섹션 추가)

**Interfaces:**
- Consumes: `IndexNowJob` (Task 3), `Hosts::INDEX_NOW_HOSTS` (Task 1)
- Produces: `Article` after_commit on create/update → per-(host,id) 캐시 잠금 + `IndexNowJob.set(wait: 30s).perform_later(id, host)`. 캐시 키 포맷 `"index_now:enqueue:#{host}:#{id}"`, TTL 60s.

- [ ] **Step 1: Write the failing test**

Append to `test/models/article_test.rb` (클래스 내부, 마지막 `end` 전):

```ruby
  # --- IndexNow enqueue ---

  test "confirmed 진입(create) 시 호스트 수만큼 IndexNowJob을 30s 지연 예약한다" do
    article = Article.create!(
      title: "새 기사",
      title_ko: "새 기사 한국어",
      url: "https://example.com/new-idx",
      origin_url: "https://example.com/new-idx",
      host: "example.com",
      slug: "new-idx",
      body: "본문",
      user: users(:john)
    )

    Hosts::INDEX_NOW_HOSTS.each do |host|
      assert_enqueued_with(
        job: IndexNowJob,
        args: [ article.id, host ],
        queue: "default"
      ) { }
    end
    # wait 옵션 확인: 예약된 잡의 scheduled_at이 30s 이후
    enqueued = ActiveJob::Base.queue_adapter.enqueued_jobs.select { |j| j["job_class"] == "IndexNowJob" }
    assert_equal Hosts::INDEX_NOW_HOSTS.size, enqueued.size
  end

  test "의미 없는 변경(touch only)은 IndexNowJob을 예약하지 않는다" do
    article = articles(:ruby_article)
    # watched 속성 외의 필드만 갱신 — is_related는 watched 목록에 없음
    clear_index_now_jobs!

    assert_no_enqueues_index_now do
      article.update!(is_related: !article.is_related)
    end
  end

  test "unconfirmed 기사(title_ko 누락) 저장은 예약하지 않는다" do
    article = articles(:site_only_article) # slug 있음, title_ko nil
    clear_index_now_jobs!

    assert_no_enqueues_index_now do
      article.update!(is_related: true)
    end
  end

  test "watched 속성 변경 시 confirmed 기사는 예약한다" do
    article = articles(:ruby_article)
    clear_index_now_jobs!

    assert_enqueues_index_now(count: Hosts::INDEX_NOW_HOSTS.size) do
      article.update!(title_ko: "바뀐 한국어 제목")
    end
  end

  test "디바운스: 30s 내 동일 host+id의 두 번째 예약은 스킵된다" do
    article = articles(:ruby_article)
    clear_index_now_jobs!
    Rails.cache.clear

    assert_enqueues_index_now(count: Hosts::INDEX_NOW_HOSTS.size) do
      article.update!(title_ko: "첫 변경")
    end

    # 잠금이 설정된 상태에서 두 번째 변경 — 추가 예약 없음
    assert_no_enqueues_index_now do
      article.update!(body: "두 번째 본문 변경")
    end
  end

  # 헬퍼
  def clear_index_now_jobs!
    ActiveJob::Base.queue_adapter.enqueued_jobs.delete_if { |j| j["job_class"] == "IndexNowJob" }
  end

  def assert_enqueues_index_now(count:)
    before = enqueued_index_now_count
    yield
    assert_equal count, enqueued_index_now_count - before
  end

  def assert_no_enqueues_index_now
    before = enqueued_index_now_count
    yield
    assert_equal before, enqueued_index_now_count
  end

  def enqueued_index_now_count
    ActiveJob::Base.queue_adapter.enqueued_jobs.count { |j| j["job_class"] == "IndexNowJob" }
  end
```

> 참고: `assert_enqueues_with` 블록 형태는 `assert_enqueues_with(...) { ... }` — 헬퍼 없이 직접 써도 되나, 디바운스 테스트는 "블록 실행 후 증분"을 봐야 하므로 카운트 기반 헬퍼를 사용. `ActiveJob::Base.queue_adapter`는 테스트 환경에서 `:test` 어댑터.

- [ ] **Step 2: Run test to verify it fails**

Run: `bin/rails test test/models/article_test.rb`
Expected: FAIL — `NoMethodError` for `enqueue_index_now` 또는 잡 미예약으로 assert 실패

- [ ] **Step 3: Write minimal implementation**

In `app/models/article.rb`, 콜백 영역(`after_commit_on_* :clear_rss_cache` 근처, line 111-153)에 추가:

```ruby
  after_commit :enqueue_index_now, on: [ :create, :update ]
```

private 영역(private 키워드 아래, 기존 private 메서드들 근처)에 추가:

```ruby
  INDEX_NOW_WATCHED_ATTRIBUTES = %w[slug title title_ko title_ja body summary_body summary_body_ja published_at].freeze

  #: () -> void
  def enqueue_index_now
    return unless kept?
    return if slug.blank? || title_ko.blank?
    return unless INDEX_NOW_WATCHED_ATTRIBUTES.any? { |attr| saved_change_to_attribute?(attr) }

    Hosts::INDEX_NOW_HOSTS.each do |host|
      lock_key = "index_now:enqueue:#{host}:#{id}"
      next if Rails.cache.exist?(lock_key)
      Rails.cache.write(lock_key, true, expires_in: 60.seconds)
      IndexNowJob.set(wait: 30.seconds).perform_later(id, host)
    end
  end
```

> `INDEX_NOW_WATCHED_ATTRIBUTES` 상수는 클래스 본문 상수 영역(line 6-17 근처)에 두어도 되나, 콜백과 인접시키기 위해 private 직전의 상수 영역에 배치해도 무방. Ruby는 private 영역 이후의 상수도 클래스 상수로 접근 가능(가시성은 인스턴스 메서드에만 적용). 구현 시 상수 영역(line 68-70 근처)에 두는 것을 권장.

- [ ] **Step 4: Run test to verify it passes**

Run: `bin/rails test test/models/article_test.rb`
Expected: PASS — IndexNow 섹션 5개 + 기존 전부 통과

- [ ] **Step 5: Validate**

Run: `rails 'ai:tool[validate]' files=app/models/article.rb level=rails`
Expected: no errors. 콜백 순서는 `rails 'ai:tool[callbacks]' model=Article`로 재확인 — `enqueue_index_now`가 create/update after_commit에 나타나야 함.

- [ ] **Step 6: Run full test suite to check 회귀**

Run: `bin/rails test`
Expected: 기존 테스트 회귀 없음. 기존 Article 저장 경로에 새 잡이 예약되는 부작용이 다른 테스트에 간섭하지 않는지 확인 — 간섭 시 해당 테스트에 `clear_index_now_jobs!` 또는 `Rails.cache.clear` setup 추가.

- [ ] **Step 7: Commit**

```bash
git add app/models/article.rb test/models/article_test.rb
git commit -m "Enqueue IndexNowJob on article confirmed-entry and content changes

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: ArticleBatchJob을 IndexNowService 사용으로 리팩터

**Files:**
- Modify: `app/jobs/article_batch_job.rb` (line 32, 45-82: `ping_index_now` 호출/정의)
- Test: `test/jobs/article_batch_job_test.rb` (line 22: `job.stub(:ping_index_now, nil)`)

**Interfaces:**
- Consumes: `IndexNowService#call(host:, urls:)` (Task 2), `Hosts::INDEX_NOW_HOSTS` (Task 1)
- Produces: 배치 잡이 `IndexNowService` 직접 호출. `ping_index_now` 프라이빗 메서드 제거.

- [ ] **Step 1: Read existing batch test fully to update stub**

Run: `sed -n '1,40p' test/jobs/article_batch_job_test.rb`
> 현재 line 22 `job.stub(:ping_index_now, nil)`로 ping을 무효화 중. 이를 `IndexNowService` stub으로 교체해야 함.

- [ ] **Step 2: Write the failing/updating test**

In `test/jobs/article_batch_job_test.rb`, 기존 `job.stub(:ping_index_now, nil) do` 블록을 아래로 교체:

```ruby
        service_called = []
        fake_service = Object.new
        fake_service.define_singleton_method(:call) do |host:, urls:|
          service_called << { host:, urls: }
        end
        IndexNowService.stub(:new, -> { fake_service }) do
          job.stub(:check_rate_limit, true) do
            job.stub(:sleep, nil) do
              job.perform(1.hour.from_now)
            end
          end
        end
```

> `ping_index_now`가 제거되면 기존 stub은 `NoMethodError`를 낼 수 있으므로 반드시 교체. 이 테스트는 ping 대상이 service_called로 전환됨을 검증하지 않아도 됨(기존 테스트 의도는 "배치 후 동작" 검증). 단, 새 단정 추가 권장:

블록 종료 후 추가 단정(선택, 권장):
```ruby
    # title_ko가 채워진 기사의 URL이 호스트별로 ping됐는지 확인
    pinged_hosts = service_called.map { |c| c[:host] }
    assert_includes pinged_hosts, "ruby-news.dev" if service_called.any?
```

- [ ] **Step 3: Run test to verify it fails**

Run: `bin/rails test test/jobs/article_batch_job_test.rb`
Expected: FAIL — `ping_index_now` 미정의 또는 stub 불일치

- [ ] **Step 4: Write minimal implementation**

In `app/jobs/article_batch_job.rb`:

1. line 32의 `ping_index_now(index_now_urls.uniq)`를 아래로 교체:

```ruby
    ping_index_now_via_service(index_now_urls.uniq)
```

2. `article_public_url`(line 45-52) 유지 — 배치는 ko 호스트(`ruby-news.dev`)만 ping하던 기존 동작을 서비스 경유로 유지. 단, 서비스 호출 시 호스트별 분할이 필요하면 아래 `ping_index_now_via_service`에서 처리. 기존 `article_public_url`은 `host: "ruby-news.dev"`로 고정되어 있으므로, 배치는 ko만 ping(기존 동작 보존). `ping_index_now_via_service` 정의:

```ruby
  #: (Array[String] urls) -> void
  def ping_index_now_via_service(urls)
    return if urls.blank?
    IndexNowService.new.call(host: "ruby-news.dev", urls: urls)
  end
```

3. 기존 `ping_index_now` 메서드 전체(line 54-82) 삭제.

> 배치는 기존처럼 ko(`.dev`)만 ping. ja는 Task 4의 after_commit이 담당. 이것이 의도된 범위(배치는 title_ko 번역 완료된 기사의 ko URL만 즉시 ping; ja URL은 별도 번역/공개 시점의 after_commit에서 처리).

- [ ] **Step 5: Run test to verify it passes**

Run: `bin/rails test test/jobs/article_batch_job_test.rb`
Expected: PASS

- [ ] **Step 5b: Validate**

Run: `rails 'ai:tool[validate]' files=app/jobs/article_batch_job.rb level=rails`
Expected: no errors

- [ ] **Step 6: Run full suite**

Run: `bin/rails test`
Expected: 전체 통과, 회귀 없음

- [ ] **Step 7: Commit**

```bash
git add app/jobs/article_batch_job.rb test/jobs/article_batch_job_test.rb
git commit -m "Refactor ArticleBatchJob to use IndexNowService

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 구현 후 확인 사항 (배포/런타임)

1. **`.jp` 키 파일 서빙**: 프로덕션에서 `https://ruby-news.jp/187d5ed120cc45f8869b89302011d43a.txt`가 200 응답하는지 확인. 같은 앱·같은 `public/` 공유 전제이나, 호스트 라우팅/CDN 설정에 따라 확인 필요.
2. **IndexNow 통계**: Bing Webmaster Tools > URL Submission에서 IndexNow 수신 확인.
3. **로그 모니터링**: 프로덕션 로그에서 `IndexNow ping success` / `ping failed` 빈도 확인.

## Self-Review 결과

- **Spec coverage**: 스펙의 모든 섹션(아키텍처, 게이팅/디바운스, 서비스/잡 인터페이스, ArticleBatchJob 변경, 테스트)가 Task 1-5에 매핑됨.
- **Placeholder scan**: 자리표시자 없음. 모든 코드 단계에 실제 코드 포함.
- **Type consistency**: `IndexNowService#call(host:, urls:)` 시그니처가 Task 2/3/5에서 일관. 캐시 키 포맷 `"index_now:enqueue:#{host}:#{id}"`가 Task 3/4에서 일관. `Hosts::INDEX_NOW_KEY`/`INDEX_NOW_HOSTS`가 Task 1에서 정의되고 Task 2/3/4에서 소비.