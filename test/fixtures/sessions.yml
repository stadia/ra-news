# frozen_string_literal: true

# Active session for John
john_session:
  user: john
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

# Active session for Jane
jane_session:
  user: jane
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

# Admin session
admin_session:
  user: admin
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

# Korean user session
korean_session:
  user: korean_user
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

# Session for testing
test_session:
  user: minimal_user
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

# Legacy fixture names for backwards compatibility
one:
  user: one
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"

two:
  user: two
  ip_address: "127.0.0.1"
  user_agent: "Rails Test"
